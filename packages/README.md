# Packages

This directory contains shared packages used across services and workers.

## Contents

- `types/` - @fileconverter/types - Shared TypeScript types and interfaces
- `utils/` - @fileconverter/utils - Shared utility functions (JWT, logging, errors)
- `prisma/` - @fileconverter/prisma - Database client and migrations
